<section class="bg-[#faf9f6] text-[#1a1a1a] pt-[80px] lg:pt-[100px] relative overflow-hidden">
  <!-- Background Blobs -->
  <div
    class="absolute top-[-5%] left-[-5%] w-[30%] h-[30%] bg-rose-100/30 rounded-full blur-[80px] pointer-events-none"
  ></div>
  <div
    class="absolute bottom-[10%] right-[-5%] w-[25%] h-[30%] bg-blue-50/40 rounded-full blur-[80px] pointer-events-none"
  ></div>

  <div class="mx-auto max-w-6xl px-6 py-10 lg:py-14 relative z-10">
    <div class="grid grid-cols-1 lg:grid-cols-12 gap-8 lg:gap-12 items-center">
      <!-- Left -->
      <div class="lg:col-span-7 text-center lg:text-left animate-fadeUp">
        <!-- Status Badge -->
        <div class="inline-flex items-center gap-2 px-3 py-1 rounded-full bg-emerald-50 border border-emerald-100 mb-6">
          <span class="w-2 h-2 rounded-full bg-emerald-500 animate-pulse"></span>
          <span class="text-[10px] uppercase tracking-[0.2em] text-emerald-700 font-bold">
            Open for New Opportunities
          </span>
        </div>

        <!-- Heading -->
        <h1 class="text-4xl md:text-5xl lg:text-6xl font-serif italic tracking-tight leading-[1.05] mb-6">
          <span class="block transform -skew-x-3">
            {{ section.settings.heading }}
          </span>
        </h1>

        <!-- Subtext -->
        <p class="text-base md:text-lg text-gray-600 mb-8 max-w-lg mx-auto lg:mx-0 leading-relaxed font-light">
          {{ section.settings.subtext }}
        </p>

        <!-- Buttons -->
        <div class="flex flex-wrap gap-4 justify-center lg:justify-start">
          {% if section.settings.button_text != blank %}
            <a
              href="{{ section.settings.button_link }}"
              class="btn-premium bg-black text-white shadow-xl"
            >
              <span class="relative z-10">{{ section.settings.button_text }}</span>
              <div class="btn-overlay bg-gradient-to-r from-rose-400 to-rose-600"></div>
            </a>
          {% endif %}

          {% if section.settings.portfolio_link != blank %}
            <a
              href="{{ section.settings.portfolio_link }}"
              class="btn-premium text-black border border-black/10 bg-white/60 backdrop-blur shadow-sm"
            >
              <span class="relative z-10">
                {{ section.settings.portfolio_text | default: 'View Portfolio' }}
              </span>
              <div class="btn-overlay bg-black/5"></div>
            </a>
          {% endif %}
        </div>
      </div>

      <!-- Right Image -->
      <div class="lg:col-span-5 flex justify-center lg:justify-end animate-float">
        {% if section.settings.image != blank %}
          <div class="relative group">
            <div class="relative w-64 h-72 md:w-80 md:h-[400px] overflow-hidden rounded-[2.5rem] shadow-[0_25px_50px_-12px_rgba(0,0,0,0.15)]">
              <img
                src="{{ section.settings.image | image_url: width: 800 }}"
                alt="Profile"
                class="w-full h-full object-cover transition-transform duration-[2s] group-hover:scale-105"
              >
            </div>

            <!-- Experience Card -->
            {% if section.settings.experience != blank %}
              <div class="absolute -bottom-4 -right-4 bg-white/95 backdrop-blur-md p-4 rounded-2xl shadow-lg border border-white/20 hidden sm:block">
                <p class="text-[9px] uppercase tracking-widest text-rose-600 font-bold mb-0.5">Experience</p>
                <p class="text-lg font-serif leading-none">
                  {{ section.settings.experience }}
                </p>
              </div>
            {% endif %}
          </div>
        {% endif %}
      </div>
    </div>

    <!-- Logos Section -->
    {% if section.blocks.size > 0 %}
      <div class="mt-16 pt-8 border-t border-black/5">
        <div class="flex flex-col md:flex-row items-center justify-between gap-6">
          <!-- Left -->
          <p class="text-[9px] uppercase tracking-[0.4em] text-gray-400 font-bold whitespace-nowrap">Expertise In</p>

          <!-- Right Logos -->
          <div class="flex flex-wrap gap-6 md:gap-10 items-center justify-center md:justify-end">
            {% for block in section.blocks %}
              {% if block.settings.logo != blank %}
                <div class="logo-item flex-shrink-0 transition-all duration-500 transform hover:-translate-y-1">
                  <img
                    src="{{ block.settings.logo | image_url: width: 180 }}"
                    alt="logo"
                    class="h-7 md:h-8 w-auto object-contain opacity-90 hover:opacity-100 transition duration-300"
                  >
                </div>
              {% endif %}
            {% endfor %}
          </div>
        </div>
      </div>
    {% endif %}
  </div>

  <!-- Styles -->
  <style>
    .btn-premium {
      position: relative;
      display: inline-flex;
      align-items: center;
      justify-content: center;
      padding: 0.875rem 1.75rem;
      font-weight: 600;
      border-radius: 9999px;
      overflow: hidden;
      transition: all 0.4s cubic-bezier(0.2, 0.8, 0.2, 1);
    }

    .btn-premium:hover {
      transform: scale(1.05);
    }

    .btn-premium:active {
      transform: scale(0.95);
    }

    .btn-overlay {
      position: absolute;
      inset: 0;
      opacity: 0;
      transition: opacity 0.3s ease;
    }

    .btn-premium:hover .btn-overlay {
      opacity: 1;
    }

    .animate-fadeUp {
      animation: fadeUp 1s cubic-bezier(0.2, 0.8, 0.2, 1) forwards;
    }

    @keyframes fadeUp {
      from {
        opacity: 0;
        transform: translateY(30px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    .animate-float {
      animation: float 6s ease-in-out infinite;
    }

    @keyframes float {
      0%,
      100% {
        transform: translateY(0px);
      }
      50% {
        transform: translateY(-15px);
      }
    }
  </style>

  {% schema %}
{
  "name": "Premium Job Banner",
  "settings": [
    { "type": "text", "id": "heading", "label": "Heading", "default": "Building Digital Excellence." },
    {
      "type": "textarea",
      "id": "subtext",
      "label": "Sub Text",
      "default": "I design and develop modern, high performing websites with a focus on usability and clean design."
    },

    { "type": "header", "content": "Primary Button" },
    { "type": "text", "id": "button_text", "label": "Button Text", "default": "Hire Me" },
    { "type": "url", "id": "button_link", "label": "Button Link" },

    { "type": "header", "content": "Secondary Button" },
    { "type": "text", "id": "portfolio_text", "label": "Portfolio Text", "default": "View Portfolio" },
    { "type": "url", "id": "portfolio_link", "label": "Portfolio Link" },

    { "type": "header", "content": "Experience Card" },
    { "type": "text", "id": "experience", "label": "Experience Text", "default": "3+ Years" },

    { "type": "header", "content": "Media" },
    { "type": "image_picker", "id": "image", "label": "Profile Image" }
  ],
  "blocks": [
    {
      "type": "logo",
      "name": "Tech Logo",
      "settings": [{ "type": "image_picker", "id": "logo", "label": "Logo Image" }]
    }
  ],
  "presets": [{ "name": "Premium Job Banner" }]
}
  {% endschema %}
</section>
